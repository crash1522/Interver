version: '3.8'

services:
  myapi:
    build: .
    container_name: api-container
    command: uvicorn main:app --reload --host 0.0.0.0
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      PYTHONPATH: /app
