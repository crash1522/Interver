apiVersion: apps/v1
kind: Deployment
metadata:
  name: interver
  labels:
    app: app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: app
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: app
    spec:
      containers:
        - name: app
          image: 900478210705.dkr.ecr.ap-northeast-2.amazonaws.com/interver:4b436a8011752b122aed7057fb0a8b505ecd0eba
          imagePullPolicy: Always

---

apiVersion: v1
kind: Service
metadata:
  name: interver-app-service
spec:
  type: LoadBalancer
  selector:
    app: app
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
